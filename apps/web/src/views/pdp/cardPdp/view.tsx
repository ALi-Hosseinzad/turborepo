'use client'

// import { useDataQuery } from '@/configs/httpService/reactQuery/fechData'

const CardPdpView = (): JSX.Element => {
  // const usersQuery = useDataQuery('gift-card-package/36', 'users')

  return (
    <div>
      {/* <p>{usersQuery?.data?.description}</p> */}
      o-o
    </div>
  )
}

export default CardPdpView
